import React from 'react'

export default function ProjectsHome() {
  return (
    <div>
      ProjectsHome
      {/* Show all projects (limit 5), starred projects (limit 5), and most 5 recent projects */}
    </div>
  )
}
